# =============================================================================
# CODEOWNERS — novuchat.site
# -----------------------------------------------------------------------------
# El ruleset `proteger-main` exige revisión de code owner
# (`require_code_owner_review: true`). Este archivo dice quién es.
#
# POR QUÉ SE REESCRIBIÓ (2026-09-03). La plantilla del estándar DevSecOps llega
# con equipos de marcador —@ORG/desarrollo, @ORG/plataforma, @ORG/seguridad— y
# `bootstrap-repo.sh` no los sustituye. En un repositorio personal esos equipos
# no existen, así que GitHub no encontraba ningún propietario válido y la
# exigencia de revisión no se podía satisfacer con ninguna aprobación: la PR #1
# quedó bloqueada sin motivo visible. Ver docs/pendiente-estandar-devsecops.md.
#
# Se nombra a @AndresAlberdi, no a @segurolotengopy: esta última es la cuenta
# que abre las PR, y GitHub no acepta que el autor apruebe su propio cambio.
# La separación de funciones del estándar se sostiene sobre esa distinción.
# =============================================================================

*                           @AndresAlberdi

# Rutas donde un descuido no se nota al mirar la página, y por eso conviene que
# el aviso de revisión sea explícito aunque el propietario sea el mismo.
/.github/                   @AndresAlberdi
/.github/workflows/         @AndresAlberdi
/.devsecops.yml             @AndresAlberdi
/firestore.rules            @AndresAlberdi
/firestore.indexes.json     @AndresAlberdi
/firebase.json              @AndresAlberdi
/functions/                 @AndresAlberdi
/pnpm-lock.yaml             @AndresAlberdi
