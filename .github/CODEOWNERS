# =============================================================================
# CODEOWNERS — novuchat.site
# -----------------------------------------------------------------------------
# El ruleset `proteger-main` exige revisión de code owner
# (`require_code_owner_review: true`). Este archivo dice quién es.
#
# POR QUÉ SE REESCRIBIÓ (2026-09-03). La plantilla del estándar DevSecOps llega
# con equipos de marcador —@ORG/desarrollo, @ORG/plataforma, @ORG/seguridad— y
# `bootstrap-repo.sh` no los sustituye. En un repositorio personal esos equipos
# no existen, así que GitHub no encontraba ningún propietario válido y la
# exigencia de revisión no se podía satisfacer con ninguna aprobación: la PR #1
# quedó bloqueada sin motivo visible. Ver docs/pendiente-estandar-devsecops.md.
#
# Se nombra a @AndresAlberdi, no a @segurolotengopy: esta última es la cuenta
# que abre las PR, y GitHub no acepta que el autor apruebe su propio cambio.
# La separación de funciones del estándar se sostiene sobre esa distinción.
#
# LAS DOS CUENTAS SON PROPIETARIAS (2026-09-03). Al principio solo figuraba
# @AndresAlberdi, con @segurolotengopy como autora de las PR. Funciona mientras
# la cuenta activa no se mueva, pero basta un `gh auth switch` para abrir una PR
# con la cuenta revisora y entonces NADIE puede aprobarla: GitHub no deja que el
# autor apruebe, y `require_code_owner_review` exige que sea un propietario.
# Pasó con la PR #8 y no tiene salida: hay que cerrarla y reabrirla.
#
# Con las dos cuentas como propietarias, la revisión la satisface siempre la que
# no haya escrito la PR, sea cual sea. La separación de funciones se mantiene
# —el autor sigue sin poder aprobarse—; lo que desaparece es el atasco.
# =============================================================================

*                           @AndresAlberdi @segurolotengopy

# Rutas donde un descuido no se nota al mirar la página, y por eso conviene que
# el aviso de revisión sea explícito aunque el propietario sea el mismo.
/.github/                   @AndresAlberdi @segurolotengopy
/.github/workflows/         @AndresAlberdi @segurolotengopy
/.devsecops.yml             @AndresAlberdi @segurolotengopy
/firestore.rules            @AndresAlberdi @segurolotengopy
/firestore.indexes.json     @AndresAlberdi @segurolotengopy
/firebase.json              @AndresAlberdi @segurolotengopy
/functions/                 @AndresAlberdi @segurolotengopy
/pnpm-lock.yaml             @AndresAlberdi @segurolotengopy
